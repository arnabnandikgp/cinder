[package]
name = "cinder-adapter"
version = "0.1.0"
description = "Cinder operator: residual hedging, reconciliation, and safety controls"
edition = "2021"

[dependencies]
cinder-common = { path = "../cinder-common" }
